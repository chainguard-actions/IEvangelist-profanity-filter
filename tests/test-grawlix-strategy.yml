name: Test grawlix replacement strategy
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run profanity filter with grawlix replacement strategy
        id: action
        uses: .
        with:
          token: ${{ github.token }}
          replacement-strategy: grawlix
          include-updated-note: 'false'
          include-confused-reaction: 'false'
        continue-on-error: true
      - name: Assert action ran with grawlix strategy
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "action outcome: $OUTCOME"
          # The action may succeed or fail due to no active issue/PR in workflow_dispatch context.
          # Cancelled would indicate a setup/configuration problem.
          if [ "$OUTCOME" = "cancelled" ]; then
            echo "FAIL: action was cancelled unexpectedly"
            exit 1
          fi
          echo "PASS: action ran with grawlix strategy, outcome=$OUTCOME"
